;Wed Dec  7 20:00:52 2022

; condition for if_0 with else 
push rax

push 0
je : else_label_0
; end of condition

; if body
push 4500
pop  rax

push 1000
push rax
SUB
pop  rax

jmp : else_end_label_0
; else body
: else_label_0
push 2000
pop  rax

: else_end_label_0
push rax
FOUT
push 500
push 400
ADD
push 100
FMUL
pop  rax

hlt
;end {document}
