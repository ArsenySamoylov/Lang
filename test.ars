;Tue Dec  6 19:57:43 2022

push 200
push 800
POW
pop  rax

; condition for if_0 with else 
push rax

push 0
je : else_label_0
; end of condition

; if body
push 4500
pop  rax

jmp : else_end_label_0
; else body
: else_label_0
push 2000
pop  rax

: else_end_label_0
push rax
FOUT
push 500
push 400
ADD
push 100
FMUL
pop  rax

push rax
FOUT
hlt
;end {document}
